{
  "name": "fallback-content-generator-lambda",
  "version": "1.0.0",
  "description": "Lambda function for generating fallback content when primary content analysis fails",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && zip -r fallback-content-generator.zip index.js node_modules/",
    "test": "jest"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/aws-lambda": "^8.10.130",
    "@types/node": "^20.6.2",
    "typescript": "^5.2.2"
  }
}